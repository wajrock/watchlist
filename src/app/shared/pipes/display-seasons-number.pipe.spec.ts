import { DisplaySeasonsNumberPipe } from './display-seasons-number.pipe';

describe('DisplaySeasonsNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySeasonsNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
