import { DisplayProviderPipe } from './display-provider.pipe';

describe('DisplayProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
