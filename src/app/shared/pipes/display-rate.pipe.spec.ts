import { DisplayRatePipe } from './display-rate.pipe';

describe('DisplayRatePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRatePipe();
    expect(pipe).toBeTruthy();
  });
});
