import { DisplayRuntimePipe } from './display-runtime.pipe';

describe('DisplayRuntimePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRuntimePipe();
    expect(pipe).toBeTruthy();
  });
});
