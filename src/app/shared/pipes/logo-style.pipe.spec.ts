import { LogoStylePipe } from './logo-style.pipe';

describe('LogoStylePipe', () => {
  it('create an instance', () => {
    const pipe = new LogoStylePipe();
    expect(pipe).toBeTruthy();
  });
});
