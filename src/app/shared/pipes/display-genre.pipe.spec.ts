import { DisplayGenrePipe } from './display-genre.pipe';

describe('DisplayGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
